name: doctorx
description: Medical NER with UMLS lookup for accurate information
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  getflutter: ^1.0.10
  dio: ^3.0.9
  provider: ^4.0.4
  flushbar: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.7.4
  flutter_native_splash: ^0.1.9

flutter:
  uses-material-design: true
